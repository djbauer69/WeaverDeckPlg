# v0.22.0 runtime test record

Reviewed user logs dated 2026-09-06:

- `com.pipeweaver.opendeck.sdPlugin(20260906-193607).log`
- `com.pipeweaver.opendeck.sdPlugin(20260906-194246).log`
- `com.pipeweaver.opendeck.sdPlugin(20260906-194713).log`
- `com.pipeweaver.opendeck.sdPlugin(20260906-195902).log` (physical feedback fix retest)

| Check | Evidence / result |
| --- | --- |
| Source A millisecond durations | 250, 1000 and 3000 ms fades complete with confirmed endpoints. |
| Immediate volume change | 0 ms changes complete in both directions. |
| Smart Scene ordering | 750 ms Source A fade reports 753 ms, then mute executes; Scene completes in 755 ms. |
| Restart persistence | Next startup restores the Scene's 750 ms duration and buttons' 0 ms durations. |
| Scene file export/import | Not independently established by these logs. |
| Source B | Repeated 100% to 20% over 3000 ms and 20% to 100% over 750 ms complete. |
| C922 physical input | Intermittent completion; external-change cancellations and unconfirmed final volumes. An invalid 1000% entry was rejected; failures continued after correction to 100%. |
| Volt 2 physical input | One 1500 ms completion; repeated unconfirmed endpoints at 200 and 1500 ms. Subsequent runs often start at the previous requested endpoint. |
| Physical input fix retest | Eight fades complete: both C922 and Volt 2 at 1500/200 ms, including concurrent fades on separate devices. No fade failures in the retest log. |
| Inspector clipping | Still recurs; user reports scrolling repaints Scene correctly, while short actions have no scrollbar. Screenshot shows Physical Input Fade clipped below the device selector. |

The physical command handler dispatches a device-volume message; its status is
updated separately on DeviceVolumeChanged. The review build now waits for bounded
physical readback confirmation without retrying commands. The user and retest log
confirm this fix on both physical inputs.

The latest review build removes the common wrapper iframe. Manifest actions open
their editors directly, and the old wrapper path navigates existing profiles to
the same direct editor using OpenDeck's normal load/connect handshake. Shared
Button Text, action header and compact styling are applied in place. Scene's two
remaining internal frames retain automatic sizing and use compositing. Actual
OpenDeck rendering of this change remains pending.

71 automated tests pass, including delayed physical readback, confirmation
timeouts, cancellation, suspended child-frame callbacks, direct editor settings,
the actual Physical Input Fade inspector and existing-profile navigation. These
simulations do not replace the pending WebView check.
