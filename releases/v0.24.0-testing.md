# v0.24.0 runtime test record

The user confirmed that Preview Scene displayed all 38 Default Scene steps correctly. The supplied log excerpt reported `previewScene reply: {"ok":true,"steps":38,"errors":0}`. The user separately confirmed that preview left PipeWeaver audio settings unchanged.

The initial stale-preview check failed. The follow-up listens to input/change events, shows a notice beside the edited field and labels the Preview Scene button as out of date. On 8 September 2026, the user reported that this retest worked, following instructions to edit a volume before leaving the field and refresh the preview.

The full 231946 log was not reviewed during the workspace outage; only the supplied excerpt and user confirmations support the runtime conclusions above. Offline-application, ambiguity and timing-boundary behavior have automated coverage but were not separately confirmed in this runtime retest.

Automated verification: 100 Node tests pass. The 15 existing Python migration/publishing tests also passed during v0.24.0 development. Tests cover the 38-step Scene fixture, read-only dispatch, state projection, conditions, unavailable/ambiguous resources, safe UI rendering, immediate invalidation and stale reply rejection.

Tested plugin ZIP: `pipeweaver-opendeck-plugin-v0.24.0.zip`

SHA-256: `47f6b7d5d7648619065b5fd051bf810c380c25ee7356924597ee3ea133572083`

This documentation update does not modify the tested plugin package.
