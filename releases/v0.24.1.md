# WeaverDeck v0.24.1 — icon and inspector cleanup (review build)

- Replace the shared PW-lettered icon with the upstream PipeWeaver SVG, including the plugin/category header icon. Include upstream attribution and the MIT license notice.
- Remove static Scene helper paragraphs and ready/loading descriptions. Keep section headings, controls, preview output and operation/error feedback.
- Replace PW fallback titles with short selection/offline labels.
- Make full event/settings/discovery dumps, application icon discovery, inspector redraw logs and routine Scene step-success detail opt-in through `WEAVERDECK_DEBUG=1` in OpenDeck's environment. Normal logs retain startup, preview/validation summaries, Scene start/end, skips, warnings, failures and fade results.
- Avoid serializing large diagnostic payloads in normal mode; remove a duplicate status-error dump. Audio execution and inspector repaint behavior are unchanged.

Validation: 102 Node tests pass, including quiet logging, preserved inspector visibility replies and optional verbose output. Upstream artwork was rendered and visually inspected. ZIP contents and executable modes are checked against source. Runtime confirmation of this cleanup is pending.

Install `pipeweaver-opendeck-plugin-v0.24.1.zip` directly through OpenDeck on the existing v0.23/v0.24 setup.

SHA-256: `bfa0e97eec6a2354354af963a3e3ce6eb83791ff7fef2bf2955915f982592107`
