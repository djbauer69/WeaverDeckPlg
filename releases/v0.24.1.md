# WeaverDeck v0.24.1 — icon and inspector cleanup

- Replace the shared PW-lettered icon with the upstream PipeWeaver SVG, including the plugin/category header icon. Use it for Audio Buffer Size and Audio Engine Restart in the action list and on new and existing buttons. Include upstream attribution and the MIT license notice.
- Remove static Scene helper paragraphs and ready/loading descriptions. Keep section headings, controls, preview output and operation/error feedback.
- Replace PW fallback titles with short selection/offline labels.
- Make full event/settings/discovery dumps, application icon discovery, inspector redraw logs and routine Scene step-success detail opt-in through `WEAVERDECK_DEBUG=1` in OpenDeck's environment. Normal logs retain startup, preview/validation summaries, Scene start/end, skips, warnings, failures and fade results.
- Avoid serializing large diagnostic payloads in normal mode; remove a duplicate status-error dump. Audio execution and inspector repaint behavior are unchanged.

Validation: 103 Node tests pass, including existing Audio button artwork, quiet logging, preserved inspector visibility replies and optional verbose output. 20 Python tests pass, including publication checksum/source checks and stopping before publication on a corrupt download. Upstream artwork was rendered and visually inspected. ZIP contents and executable modes are checked against source. The v0.24.0 preview was user-tested; this artwork/logging cleanup has automated and source validation, without a new user runtime confirmation.

Install `pipeweaver-opendeck-plugin-v0.24.1.zip` directly through OpenDeck on the existing v0.23/v0.24 setup.

SHA-256: `9c9adc5c6c0fa29c5b674aa1b4e34832ce4e2ffe49e04c38a94895a870cb4734`
