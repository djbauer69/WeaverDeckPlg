# v0.23.0 runtime test record

On 7 September 2026, djbauer confirmed that all requested grouped-action checks were working. The supplied plugin log was `com.pipeweaver.opendeck.sdPlugin(20260907-223319).log`; the raw log is not published because it contains local device identifiers and settings.

## User-confirmed results

- Operation selection across Application, Physical Input, Physical Output, Route, Source and Target.
- Retention of selections, numeric settings and manual text across operation changes and restart.
- Physical Input/Output icon choices, mute states and volume percentage badges.
- Tap, hold, release, Set Volume where available, and fade interruption.
- Source A/B volume/mute, Volume Link and Mute To controls.
- Inspector visibility after deletion, empty-key selection and reselection.
- Route, Target Mix and Target Mute original artwork restored by the final icon-path fix.

## Log evidence

The log identifies v0.23.0 and shows a successful OpenDeck connection. Nine fades complete: Spotify, physical input, physical output, Source Music A/B and Target Speakers are represented. Three fades stop with the reason “Fade cancelled by another control or disconnect”, consistent with interruption testing; the log does not distinguish those causes. No crashes or timeout errors appear.

Appearance, audible results and complete settings retention rely on the user's confirmation, rather than inference from the log. This records the tested installation, not universal platform coverage.

## Automated verification and package

89 Node tests and 15 Python tests pass. Icon-path regression checks match OpenDeck's extension resolution; conversion tests cover metadata recognition, exact backups and rollback.

Tested plugin ZIP: `pipeweaver-opendeck-plugin-v0.23.0.zip`

SHA-256: `dee3cab92dd62561e01de4e64778f0d749ddb9518a23c5bd7545bf4602a79337`

Documentation updates do not change the tested runtime ZIP. Subsequent installations on the already-migrated test setup use the plugin ZIP directly through OpenDeck.
