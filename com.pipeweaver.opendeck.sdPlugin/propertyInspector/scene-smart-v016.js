"use strict";
let v016DeckArgs=null,v016Base=null,v016Retry=null;
function connectElgatoStreamDeckSocket(...args){v016DeckArgs=args;if(v016Base?.connectElgatoStreamDeckSocket)v016Base.connectElgatoStreamDeckSocket(...args)}
function v016FrameReady(){v016Base=document.getElementById("baseFrame").contentWindow;if(v016DeckArgs&&v016Base?.connectElgatoStreamDeckSocket)v016Base.connectElgatoStreamDeckSocket(...v016DeckArgs);v016InstallSmartHooks()}
function v016InstallSmartHooks(){
  try{
    const inner=v016Base?.document?.getElementById("sceneFrame")?.contentWindow;
    // scene-v0151.html sets __weaver0151 directly on the child window after
    // its frameReady() sequence has installed the v0.15.1 bridge/hooks.
    // Waiting on __weaver0151sourceLinks was too strict in OpenDeck's WebView
    // and could leave this wrapper polling forever even though the base Scene
    // Builder was already fully usable.
    if(!inner||!inner.document||!inner.__weaver0151||typeof inner.render!=="function"||typeof inner.fields!=="function"||typeof inner.summary!=="function"){
      const status=document.getElementById("smartStatus");
      if(status)status.textContent="Waiting for Scene Builder controls…";
      v016Retry=setTimeout(v016InstallSmartHooks,50);
      return;
    }
    if(inner.__weaver016smartScenes){
      const status=document.getElementById("smartStatus");
      if(status)status.textContent="Smart Scene controls ready.";
      return;
    }
    const s=inner.document.createElement("script");
    s.textContent="(()=>{\nif(window.__weaver016smartScenes)return;\nwindow.__weaver016smartScenes=true;\nfunction smartCondition(op){\n  const raw=op?.condition;\n  if(!raw||typeof raw!==\"object\"||Array.isArray(raw))return {type:\"always\",application:null};\n  return {type:String(raw.type||\"always\"),application:raw.application&&typeof raw.application===\"object\"?raw.application:null};\n}\nfunction smartProcessKey(v){return String(v||\"\").trim().replace(/\\s+\\(deleted\\)$/i,\"\").toLowerCase()}\nfunction smartAppKey(a){return a?`${String(a.deviceType||\"\").toLowerCase()}|${smartProcessKey(a.process)}|${a.name||\"\"}`:\"\"}\nfunction smartConditionLabel(op){\n  const c=smartCondition(op);\n  if(c.type===\"always\")return \"Always\";\n  const d=c.application||{},label=`${d.name||\"Application\"}${d.process?` (${d.process})`:\"\"}${d.deviceType?` [${d.deviceType}]`:\"\"}`;\n  return c.type===\"applicationNotRunning\"?`If ${label} is not running`:`If ${label} is running`;\n}\nfunction smartConditionAppSelect(i,op){\n  const c=smartCondition(op),current=c.application||null,list=Array.isArray(data.applications)?data.applications:[];\n  const currentKey=smartAppKey(current),matched=list.findIndex(a=>smartAppKey(a)===currentKey);\n  let html=\"\";\n  if(current&&matched<0)html+=`<option value=\"-1\" selected>Configured: ${esc(current.name||\"Application\")}${current.process?` (${esc(current.process)})`:\"\"}${current.deviceType?` [${esc(current.deviceType)}]`:\"\"}</option>`;\n  list.forEach((a,j)=>{const selected=matched===j?\" selected\":\"\";html+=`<option value=\"${j}\"${selected}>${esc(a.name||\"Application\")}${a.process?` (${esc(a.process)})`:\"\"}${a.deviceType?` [${esc(a.deviceType)}]`:\"\"}</option>`});\n  if(!html)html='<option value=\"\">No live applications — configure when one is running</option>';\n  return `<select onchange=\"setSmartConditionApplication(${i},this.value)\">${html}</select>`;\n}\nwindow.setSmartConditionType=function(i,v){\n  const op=operations[i];if(!op)return;\n  if(v===\"always\"){op.condition={type:\"always\"};save();render();return}\n  const existing=smartCondition(op).application;\n  const first=Array.isArray(data.applications)&&data.applications.length?appDescriptor(data.applications[0]):null;\n  op.condition={type:v,application:existing||first};save();render();\n};\nwindow.setSmartConditionApplication=function(i,v){\n  const op=operations[i],idx=Number(v);if(!op||!Number.isInteger(idx)||idx<0||!data.applications?.[idx])return;\n  const c=smartCondition(op);op.condition={type:c.type===\"applicationNotRunning\"?\"applicationNotRunning\":\"applicationRunning\",application:appDescriptor(data.applications[idx])};save();renderSummary(i);\n};\nwindow.setSmartFailure=function(i,v){const op=operations[i];if(!op)return;op.onFailure=v===\"continue\"?\"continue\":\"stop\";save();renderSummary(i)};\nwindow.setSmartWait=function(i,v){const op=operations[i];if(!op)return;let n=Math.round(Number(v));if(!Number.isFinite(n))n=250;n=Math.max(0,Math.min(60000,n));op.milliseconds=n;save();renderSummary(i)};\nfunction smartControls(op,i){\n  const c=smartCondition(op),failure=op?.onFailure===\"continue\"?\"continue\":\"stop\";\n  let html='<div class=\"field\"><label>Condition</label><select onchange=\"setSmartConditionType('+i+',this.value)\">'+\n    '<option value=\"always\" '+(c.type===\"always\"?'selected':'')+'>Always</option>'+\n    '<option value=\"applicationRunning\" '+(c.type===\"applicationRunning\"?'selected':'')+'>Application running</option>'+\n    '<option value=\"applicationNotRunning\" '+(c.type===\"applicationNotRunning\"?'selected':'')+'>Application not running</option></select></div>';\n  if(c.type===\"applicationRunning\"||c.type===\"applicationNotRunning\")html+='<div class=\"field\"><label>Condition app</label>'+smartConditionAppSelect(i,op)+'</div>';\n  if(op?.type!==\"wait\")html+='<div class=\"field\"><label>On failure</label><select onchange=\"setSmartFailure('+i+',this.value)\"><option value=\"stop\" '+(failure===\"stop\"?'selected':'')+'>Stop Scene</option><option value=\"continue\" '+(failure===\"continue\"?'selected':'')+'>Continue Scene</option></select></div>';\n  html+='<div class=\"appnote\">Conditions are evaluated immediately before this step. A condition that is not met skips the step without failing the Scene.</div>';\n  return html;\n}\nconst previousSetType=setType;\nsetType=function(i,v){\n  const old=operations[i]||{},condition=old.condition,onFailure=old.onFailure;\n  if(v===\"wait\"){\n    operations[i]={type:\"wait\",milliseconds:250,condition:condition||{type:\"always\"},onFailure:onFailure||\"stop\"};render();save();return;\n  }\n  previousSetType(i,v);\n  if(operations[i]){if(condition)operations[i].condition=condition;if(onFailure)operations[i].onFailure=onFailure;save();render()}\n};\nconst previousSummary=summary;\nsummary=function(op){\n  let base=op?.type===\"wait\"?`Wait ${Math.max(0,Math.min(60000,Math.round(Number(op.milliseconds)||0)))} ms`:previousSummary(op);\n  const cond=smartConditionLabel(op),failure=op?.onFailure===\"continue\"?\" • Continue on failure\":\"\";\n  return `${base} • ${cond}${failure}`;\n};\nconst previousFields=fields;\nfields=function(op,i){\n  const base=op?.type===\"wait\"?`<div class=\"field\"><label>Wait</label><input type=\"number\" min=\"0\" max=\"60000\" step=\"50\" inputmode=\"numeric\" value=\"${esc(op.milliseconds??250)}\" onchange=\"setSmartWait(${i},this.value)\"></div><div class=\"appnote\">Pause Scene execution for up to 60 seconds. Use short waits when PipeWeaver or an application needs time to settle.</div>`:previousFields(op,i);\n  return base+smartControls(op,i);\n};\nconst previousRender=render;\nrender=function(){\n  previousRender();\n  const sels=[...document.querySelectorAll(\".step-head select\")];\n  sels.forEach((sel,i)=>{\n    if(!sel.querySelector('option[value=\"wait\"]')){const opt=document.createElement(\"option\");opt.value=\"wait\";opt.textContent=\"Wait / Delay\";sel.appendChild(opt)}\n    if(operations[i])sel.value=operations[i].type;\n  });\n};\nrender();\n})();";
    const host=inner.document.head||inner.document.documentElement;host.appendChild(s);s.remove();
    document.getElementById("smartStatus").textContent="Smart Scene controls ready.";
  }catch(e){document.getElementById("smartStatus").textContent="Smart Scene setup failed: "+e.message;v016Retry=setTimeout(v016InstallSmartHooks,250)}
}
